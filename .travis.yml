language: c
sudo: required
install: wget https://raw.githubusercontent.com/thpani/ocaml-ci-scripts/master/.travis-ocaml.sh
script: LD_LIBRARY_PATH=$HOME/.opam/${OCAML_VERSION}.1/lib/z3:$LD_LIBRARY_PATH travis_wait 60 bash -ex .travis-opam.sh
env:
  - OCAML_VERSION=4.06 OPAM_VERSION=1.2.2
os:
  - linux
