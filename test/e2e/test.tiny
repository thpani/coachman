while p = p do
  p := p;
  z := z;
  if a = a then
    a:=a;
    if s = s then
      s := s;
    else
      t := t;
    fi
    z:=z;
    while k = k do
        l := l;
    od
    z:=z;
    if u = u then
      u := u;
    else
      v := v;
    fi
  fi
  p := p;
od
